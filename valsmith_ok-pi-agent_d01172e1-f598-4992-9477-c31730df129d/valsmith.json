{
    "benchmark_id": "d01172e1-f598-4992-9477-c31730df129d",
    "benchmark_name": "valsmith",
    "started_at": "2026-07-21T18:22:28.500598",
    "finished_at": "2026-07-21T18:32:54.880841",
    "status": "FINISHED",
    "benchmark_arguments": {
        "contract": {
            "name": "ok-pi-agent",
            "install_cmd": "bash /bundle/ok-pi-agent/setup.sh",
            "run_cmd": "/usr/local/bin/python3 /bundle/ok-pi-agent/run_agent.py --problem-statement '{problem_statement_path}' --task-id '{task_id}' --timeout-seconds 1800",
            "final_output": "/logs/ok-pi-agent",
            "output_artifacts": [],
            "egress_allowlist": [],
            "secrets": {
                "PI_CODEX_AUTH_JSON_B64": "ok-pi-agent-codex-auth-json-b64"
            },
            "kwargs": {}
        },
        "concurrency": 1,
        "task_ids": [
            "vals-ai-valkyrie-7ad2042d37a9"
        ],
        "dataset": "s3://agentic-harness/benchmarks/valsmith-datasets/vals-ai-valkyrie/665f2b07-3586-45e7-ae18-c869b676952e/manifest.json",
        "sandbox_provider": "daytona",
        "sandbox_provider_secret_name": "AgenticHarnessSecrets"
    },
    "final_evaluation": {
        "final_score": 100.0,
        "id": "5bb64733-39d8-4034-b0f3-9174bfbb0c7e",
        "benchmark": "d01172e1-f598-4992-9477-c31730df129d",
        "properties": {
            "resolved_tasks": [
                "vals-ai-valkyrie-7ad2042d37a9"
            ],
            "unresolved_tasks": []
        },
        "org_id": "02aaff50-a972-4095-a1ba-73ba5b268c55"
    },
    "average_task_breakdown": {
        "sandbox_build_duration": 0.9767710920132231,
        "agent_run_duration": 515.39974333,
        "evaluation_run_duration": 8.643410923978081,
        "sandbox_run_duration": 616.7340568169893
    },
    "evaluation_results": {
        "vals-ai-valkyrie-7ad2042d37a9": {
            "resolved": true,
            "fail_to_pass_passed": 1,
            "fail_to_pass_total": 1,
            "fail_to_pass_failed": [],
            "pass_to_pass_failed": [],
            "tests_parsed": 19,
            "prediction": "diff --git a/services/tracker/main.py b/services/tracker/main.py\nindex e800be6..1ad44cf 100644\n--- a/services/tracker/main.py\n+++ b/services/tracker/main.py\n@@ -7,7 +7,7 @@ from uuid import UUID\n import httpx\n import logfire\n import sentry_sdk\n-from benchmark_service.client import BenchmarkServiceError\n+from benchmark_service.client import BenchmarkServiceError, BenchmarkServiceUnauthenticatedError\n from benchmark_service.schemas import VerifyTaskIdsResponse\n from fastapi import Body, Depends, FastAPI, HTTPException, Query, Request\n from fastapi.responses import StreamingResponse\n@@ -125,6 +125,12 @@ async def tracker_service_error_handler(_request: Request, exc: TrackerServiceEr\n     raise HTTPException(status_code=500, detail=str(exc)) from exc\n \n \n+@app.exception_handler(BenchmarkServiceUnauthenticatedError)\n+async def benchmark_service_unauthenticated_error_handler(_request: Request, exc: BenchmarkServiceUnauthenticatedError):\n+    logger.error(\"Benchmark service authentication failed: %s\", exc)\n+    raise HTTPException(status_code=502, detail=str(exc)) from exc\n+\n+\n @app.exception_handler(BenchmarkServiceError)\n async def benchmark_service_error_handler(_request: Request, exc: BenchmarkServiceError):\n     logger.error(exc, exc_info=True)\n@@ -199,6 +205,7 @@ async def start_benchmark(\n     Returns:\n     - 200 OK if benchmark starts successfully\n     - 400 Bad Request if parameters are invalid\n+    - 502 Bad Gateway if the benchmark service is unreachable or rejects authentication\n     - 500 Internal Server Error if benchmark fails to start\n     \"\"\"\n     request = request.model_copy(\n@@ -247,6 +254,9 @@ async def start_benchmark(\n             request.harness_config.aws,\n             request.harness_config.s3_bucket,\n         )\n+    except BenchmarkServiceUnauthenticatedError as e:\n+        commit_benchmark_error(benchmark_row, session, str(e))\n+        raise\n     except Exception as e:\n         error_message = f\"{str(e)}\\n{traceback.format_exc()}\"\n         commit_benchmark_error(benchmark_row, session, error_message)\n@@ -308,7 +318,7 @@ async def fetch_benchmark_tasks(\n             )\n         finally:\n             await benchmark_service.close()\n-    except (BenchmarkServiceError, httpx.HTTPError) as exc:\n+    except httpx.HTTPError as exc:\n         raise HTTPException(\n             status_code=502,\n             detail=f\"Failed to fetch task ids from benchmark service '{request.benchmark_name}': {exc}\",\ndiff --git a/services/tracker/tests/unit/test_benchmark_utils.py b/services/tracker/tests/unit/test_benchmark_utils.py\nindex 9c807f2..7f9917a 100644\n--- a/services/tracker/tests/unit/test_benchmark_utils.py\n+++ b/services/tracker/tests/unit/test_benchmark_utils.py\n@@ -4,7 +4,11 @@ from uuid import uuid4\n from zoneinfo import ZoneInfo\n \n import pytest\n-from benchmark_service.client import BenchmarkServiceClient, BenchmarkServiceError\n+from benchmark_service.client import (\n+    BenchmarkServiceClient,\n+    BenchmarkServiceError,\n+    BenchmarkServiceUnauthenticatedError,\n+)\n from benchmark_service.schemas import FinalScoreResponse, VerifyTaskIdsResponse\n from httpx._models import Response\n from sqlmodel import Session, col, func, select, update\n@@ -323,6 +327,35 @@ class TestBenchmarkUtils:\n         assert len(task_rows) == 5\n         assert all(task_row.status == TaskStatus.PENDING for task_row in task_rows)\n \n+    def test_retry_or_resume_returns_502_for_authentication_errors_without_sentry(\n+        self,\n+        example_benchmark_object: Benchmark,\n+        database_session: Session,\n+        monkeypatch: pytest.MonkeyPatch,\n+    ):\n+        benchmark_row = example_benchmark_object\n+        benchmark_row.status = BenchmarkStatus.STOPPED\n+        database_session.add(benchmark_row)\n+        database_session.add(\n+            Task(org_id=TEST_ORG_ID, task_id=\"task_0\", benchmark=benchmark_row.id, status=TaskStatus.STOPPED)\n+        )\n+        database_session.commit()\n+\n+        error = BenchmarkServiceUnauthenticatedError(\"credentials rejected\")\n+\n+        async def _mock_verify_task_ids(*_args: Any, **_kwargs: Any) -> VerifyTaskIdsResponse:\n+            raise error\n+\n+        captured_exceptions: list[Exception] = []\n+        monkeypatch.setattr(BenchmarkServiceClient, \"verify_task_ids\", _mock_verify_task_ids)\n+        monkeypatch.setattr(\"main.sentry_sdk.capture_exception\", captured_exceptions.append)\n+\n+        response = client.post(f\"/retry-or-resume-benchmark/{benchmark_row.id}?retry=false\")\n+\n+        assert response.status_code == 502\n+        assert response.json() == {\"detail\": str(error)}\n+        assert captured_exceptions == []\n+\n     def test_create_task_rows(self, example_benchmark_object: Benchmark, database_session: Session):\n         \"\"\"\n         Tests different scenarios for creating task rows\ndiff --git a/services/tracker/tests/unit/test_fastapi_server.py b/services/tracker/tests/unit/test_fastapi_server.py\nindex 75994c9..10d7b21 100644\n--- a/services/tracker/tests/unit/test_fastapi_server.py\n+++ b/services/tracker/tests/unit/test_fastapi_server.py\n@@ -8,7 +8,11 @@ from uuid import UUID, uuid4\n import pytest\n \n import httpx\n-from benchmark_service.client import BenchmarkServiceClient\n+from benchmark_service.client import (\n+    BenchmarkServiceClient,\n+    BenchmarkServiceError,\n+    BenchmarkServiceUnauthenticatedError,\n+)\n from benchmark_service.schemas import FinalScoreResponse, VerifyTaskIdsResponse\n from dateutil.parser import isoparse\n from descope import DescopeClient\n@@ -82,6 +86,50 @@ class TestFastapiServer:\n         assert response.status_code == 200\n         assert response.json() == {\"task_ids\": [\"task_1\", \"task_2\"]}\n \n+    async def test_fetch_benchmark_tasks_returns_502_for_authentication_errors_without_sentry(\n+        self,\n+        monkeypatch: MonkeyPatch,\n+    ):\n+        error = BenchmarkServiceUnauthenticatedError(\"credentials rejected\")\n+\n+        async def _mock_verify_task_ids(*_args: Any, **_kwargs: Any) -> VerifyTaskIdsResponse:\n+            raise error\n+\n+        capture_exception = MagicMock()\n+        monkeypatch.setattr(BenchmarkServiceClient, \"verify_task_ids\", _mock_verify_task_ids)\n+        monkeypatch.setattr(\"main.sentry_sdk.capture_exception\", capture_exception)\n+\n+        response = client.post(\n+            \"/fetch-benchmark-tasks\",\n+            json={\"benchmark_name\": \"swebench\", \"dataset\": \"verified\"},\n+        )\n+\n+        assert response.status_code == 502\n+        assert response.json() == {\"detail\": str(error)}\n+        capture_exception.assert_not_called()\n+\n+    async def test_fetch_benchmark_tasks_keeps_generic_benchmark_errors_as_internal_errors(\n+        self,\n+        monkeypatch: MonkeyPatch,\n+    ):\n+        error = BenchmarkServiceError(\"benchmark service failed\")\n+\n+        async def _mock_verify_task_ids(*_args: Any, **_kwargs: Any) -> VerifyTaskIdsResponse:\n+            raise error\n+\n+        capture_exception = MagicMock()\n+        monkeypatch.setattr(BenchmarkServiceClient, \"verify_task_ids\", _mock_verify_task_ids)\n+        monkeypatch.setattr(\"main.sentry_sdk.capture_exception\", capture_exception)\n+\n+        response = client.post(\n+            \"/fetch-benchmark-tasks\",\n+            json={\"benchmark_name\": \"swebench\", \"dataset\": \"verified\"},\n+        )\n+\n+        assert response.status_code == 500\n+        assert response.json() == {\"detail\": str(error)}\n+        capture_exception.assert_called_once_with(error)\n+\n     async def test_start_benchmark(\n         self,\n         contract: AgentContractRequest,\n@@ -175,6 +223,34 @@ class TestFastapiServer:\n         assert response.status_code == 502\n         assert response.json() == {\"detail\": \"Benchmark service 'swebench' is not reachable\"}\n \n+    async def test_start_benchmark_returns_502_for_authentication_errors_without_sentry(\n+        self,\n+        contract: AgentContractRequest,\n+        monkeypatch: MonkeyPatch,\n+        harness_config: HarnessConfig,\n+    ):\n+        request = StartBenchmarkRequest(\n+            contract=contract,\n+            benchmark_name=\"swebench\",\n+            concurrency=10,\n+            task_ids=None,\n+            harness_config=harness_config,\n+        )\n+        error = BenchmarkServiceUnauthenticatedError(\"credentials rejected\")\n+\n+        async def _mock_verify_task_ids(*_args: Any, **_kwargs: Any) -> VerifyTaskIdsResponse:\n+            raise error\n+\n+        capture_exception = MagicMock()\n+        monkeypatch.setattr(BenchmarkServiceClient, \"verify_task_ids\", _mock_verify_task_ids)\n+        monkeypatch.setattr(\"main.sentry_sdk.capture_exception\", capture_exception)\n+\n+        response = client.post(\"/start-benchmark\", json=request.model_dump())\n+\n+        assert response.status_code == 502\n+        assert response.json() == {\"detail\": str(error)}\n+        capture_exception.assert_not_called()\n+\n     async def test_start_benchmark_forwards_tracker_api_key_to_benchmark_service(\n         self,\n         contract: AgentContractRequest,\n",
            "agent_caused_exit_reason": null,
            "task_breakdown": {
                "evaluation_run_duration": 8.643410923978081,
                "sandbox_run_duration": 616.7340568169893,
                "sandbox_build_duration": 0.9767710920132231,
                "agent_run_duration": 515.39974333
            },
            "attempts": 1
        }
    }
}