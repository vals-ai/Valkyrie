[project]
name = "valkyrie-executor-artifact"
version = "0.1.0"
description = "Locked runtime dependencies for immutable executor PEX artifacts"
requires-python = "==3.12.*"
dependencies = ["tracker"]

[tool.uv]
prerelease = "allow"

[tool.uv.sources]
tracker = { path = "../tracker" }
create-benchmark-service = { git = "https://github.com/vals-ai/create-benchmark-service.git", rev = "658b08415c4976823e17f289fc7247b84013efd0" }
