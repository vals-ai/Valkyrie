[project]
name = "valkyrie-executor-artifact"
version = "0.1.0"
description = "Locked runtime dependencies for immutable executor PEX artifacts"
requires-python = "==3.12.*"
dependencies = ["tracker"]

[tool.uv]
prerelease = "allow"

[tool.uv.sources]
create-benchmark-service = { git = "https://github.com/vals-ai/create-benchmark-service.git", rev = "ff4da4c352893a2f6ad77157a31d25ec1616c525" }
tracker = { path = "../tracker" }
