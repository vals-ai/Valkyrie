[project]
name = "valkyrie-executor-host"
version = "0.1.0"
description = "Stable host for content-addressed Valkyrie executor artifacts"
requires-python = "==3.12.*"
dependencies = [
  "boto3==1.43.0",
  "psycopg2-binary==2.9.11",
  "taskiq==0.12.1",
  "taskiq-redis==1.2.2",
]

[tool.uv]
package = false
