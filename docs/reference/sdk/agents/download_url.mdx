---
title: "client.agents.download_url"
description: "Create a temporary download URL for an uploaded agent."
---

{/* Generated by scripts/generate_reference.py; do not edit. */}

Create a temporary download URL for an uploaded agent.

<Panel>
<CodeGroup>

```python Signature
async def download_url(name: str) -> AgentDownloadURLResponse
```

```bash HTTP
curl --request GET "$VALKYRIE_BASE_URL/agents/agent%20one/download-url" \
  --header "X-Api-Key: $VALS_API_KEY" \
  --header "X-Harness-Aws-Access-Key-Id: $AWS_ACCESS_KEY_ID" \
  --header "X-Harness-Aws-Secret-Access-Key: $AWS_SECRET_ACCESS_KEY" \
  --header "X-Harness-Aws-Default-Region: $AWS_DEFAULT_REGION" \
  --header "X-Harness-S3-Bucket: $S3_BUCKET" \
  --header "X-Harness-Log-Group: $LOG_GROUP" \
  --header "X-Harness-Log-Retention-Policy: $LOG_RETENTION_POLICY"
# For temporary credentials, add:
#   --header "X-Harness-Aws-Session-Token: $AWS_SESSION_TOKEN"
```

</CodeGroup>
</Panel>

**HTTP:** Raw cURL callers must URL-encode the agent-name path segment; the SDK does this automatically.

## Parameters

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">name</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · str</span></div>
</div>

## Returns

<a href="/reference/sdk/models/agents/agent-download-url-response"><code>AgentDownloadURLResponse</code></a>

For examples, see the [Python SDK guide](/sdk/resources).
