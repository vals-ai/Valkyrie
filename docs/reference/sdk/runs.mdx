---
title: "Runs resource"
description: "Async operations for the Valkyrie run lifecycle."
---

{/* Generated by scripts/generate_reference.py; do not edit. */}

Access these methods through `client.runs`.

## `client.runs.start` {#start}

Start a run from an uploaded agent name or complete contract.

```python Signature
async def start(
    agent: str | AgentContractRequest,
    benchmark: str,
    *,
    model: str | None = None,
    concurrency: int = 5,
    priority: int | None = None,
    properties: AWSResources | None = None,
    managed_s3_bucket: str | None = None,
    task_ids: Sequence[str] | None = None,
    slice_str: str | None = None,
    dataset: str | None = None,
    label: str | None = None,
    lambda_function: str | None = None,
    provider: str | None = None,
    agent_kwargs: Mapping[str, str] | None = None,
    secrets: Mapping[str, str] | None = None,
    service_headers: Mapping[str, str] | None = None,
    webhook_intervals: Sequence[int] | None = None,
    ignore_custom_services: bool = False,
) -> StartRunResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"agent"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · str | <a href="/reference/sdk/models/agents#agent-contract-request"><code>AgentContractRequest</code></a></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"benchmark"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · str</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"model"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · str | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"concurrency"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int · default: <code>{"5"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"priority"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"properties"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · AWSResources | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"managed_s3_bucket"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · str | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"task_ids"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Sequence[str] | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"slice_str"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · str | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"dataset"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · str | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"label"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · str | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"lambda_function"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · str | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"provider"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · str | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"agent_kwargs"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Mapping[str, str] | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"secrets"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Mapping[str, str] | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"service_headers"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Mapping[str, str] | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"webhook_intervals"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Sequence[int] | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"ignore_custom_services"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · bool · default: <code>{"False"}</code></span></div>
</div>

**Returns**

<a href="/reference/sdk/models/runs#start-run-response"><code>StartRunResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.fetch` {#fetch}

Fetch the latest state of a run.

```python Signature
async def fetch(run_id: UUID) -> GetRunResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

**Returns**

<a href="/reference/sdk/models/runs#get-run-response"><code>GetRunResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.filter_options` {#filter-options}

Discover valid run-filter values from this organization's run history.

```python Signature
async def filter_options() -> FilterOptionsResponse
```

**Returns**

<a href="/reference/sdk/models/runs#filter-options-response"><code>FilterOptionsResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.list` {#list}

List runs using typed filters and pagination.

```python Signature
async def list(request: ListRunsRequest | None = None) -> ListRunsResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"request"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <a href="/reference/sdk/models/runs#list-runs-request"><code>ListRunsRequest</code></a> | None · default: <code>{"None"}</code></span></div>
</div>

**Returns**

<a href="/reference/sdk/models/runs#list-runs-response"><code>ListRunsResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.statuses` {#statuses}

Fetch lightweight status and task counts for several runs.

```python Signature
async def statuses(run_ids: Sequence[UUID]) -> RunStatusResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_ids"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · Sequence[UUID]</span></div>
</div>

**Returns**

<a href="/reference/sdk/models/runs#run-status-response"><code>RunStatusResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.tasks` {#tasks}

Fetch a filtered and paginated page of tasks for one run.

```python Signature
async def tasks(run_id: UUID, request: FetchTasksRequest | None = None) -> TasksResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"request"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <a href="/reference/sdk/models/benchmarks#fetch-tasks-request"><code>FetchTasksRequest</code></a> | None · default: <code>{"None"}</code></span></div>
</div>

**Returns**

<a href="/reference/sdk/models/benchmarks#tasks-response"><code>TasksResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.iter_tasks` {#iter-tasks}

Iterate matching tasks from the requested offset.

```python Signature
async def iter_tasks(run_id: UUID, request: FetchTasksRequest | None = None) -> AsyncIterator[TaskSummary]
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"request"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <a href="/reference/sdk/models/benchmarks#fetch-tasks-request"><code>FetchTasksRequest</code></a> | None · default: <code>{"None"}</code></span></div>
</div>

**Returns**

AsyncIterator[<a href="/reference/sdk/models/benchmarks#task-summary"><code>TaskSummary</code></a>]

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.task` {#task}

Fetch detailed state and evaluation output for one task.

```python Signature
async def task(run_id: UUID, task_id: str) -> SingleTaskResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"task_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · str</span></div>
</div>

**Returns**

<a href="/reference/sdk/models/benchmarks#single-task-response"><code>SingleTaskResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.artifacts` {#artifacts}

Fetch temporary output and log links for one task.

```python Signature
async def artifacts(run_id: UUID, task_id: str) -> TaskArtifactsResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"task_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · str</span></div>
</div>

**Returns**

<a href="/reference/sdk/models/benchmarks#task-artifacts-response"><code>TaskArtifactsResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.iter` {#iter}

Iterate matching runs using cursor pagination, starting at the supplied cursor.

Use list() for explicit offset pagination. Filters and page size are preserved.

```python Signature
async def iter(request: ListRunsRequest | None = None) -> AsyncIterator[RunSummary]
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"request"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <a href="/reference/sdk/models/runs#list-runs-request"><code>ListRunsRequest</code></a> | None · default: <code>{"None"}</code></span></div>
</div>

**Returns**

AsyncIterator[<a href="/reference/sdk/models/runs#run-summary"><code>RunSummary</code></a>]

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.stream` {#stream}

Yield typed updates until the run completes or disconnects.

```python Signature
async def stream(run_id: UUID) -> AsyncIterator[GetRunResponse]
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

**Returns**

AsyncIterator[<a href="/reference/sdk/models/runs#get-run-response"><code>GetRunResponse</code></a>]

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.results` {#results}

Fetch final results or upload them and return S3 links.

```python Overload 1
async def results(
    run_id: UUID,
    *,
    task_ids: Sequence[str] | None = None,
    upload_to_s3: Literal[False] = False,
) -> RunResultsResponse
```

```python Overload 2
async def results(
    run_id: UUID,
    *,
    task_ids: Sequence[str] | None = None,
    upload_to_s3: Literal[True],
) -> S3UploadResultsResponse
```

```python Overload 3
async def results(
    run_id: UUID,
    *,
    task_ids: Sequence[str] | None = None,
    upload_to_s3: bool,
) -> RetrieveRunResultsResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"task_ids"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Sequence[str] | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"upload_to_s3"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · bool · default: <code>{"False"}</code></span></div>
</div>

**Returns**

The selected overload determines the return type.

| Overload | Return type |
| ---: | --- |
| 1 | <a href="/reference/sdk/models/runs#run-results-response"><code>RunResultsResponse</code></a> |
| 2 | <a href="/reference/sdk/models/runs#s3-upload-results-response"><code>S3UploadResultsResponse</code></a> |
| 3 | RetrieveRunResultsResponse |

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.preview` {#preview}

Archive the current S3 results, replace them with a fresh snapshot, and run the optional callback.

```python Signature
async def preview(run_id: UUID, *, task_ids: Sequence[str] | None = None) -> S3UploadResultsResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"task_ids"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Sequence[str] | None · default: <code>{"None"}</code></span></div>
</div>

**Returns**

<a href="/reference/sdk/models/runs#s3-upload-results-response"><code>S3UploadResultsResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.metadata` {#metadata}

Fetch the stored launch metadata for a run.

```python Signature
async def metadata(run_id: UUID) -> RunMetadataResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

**Returns**

<a href="/reference/sdk/models/runs#run-metadata-response"><code>RunMetadataResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.results_exist` {#results-exist}

Check whether the canonical result file already exists in S3.

```python Signature
async def results_exist(run_id: UUID) -> ResultsExistResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

**Returns**

<a href="/reference/sdk/models/runs#results-exist-response"><code>ResultsExistResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.analyze` {#analyze}

Yield typed progress events while analyzing a finished run.

```python Signature
async def analyze(
    run_id: UUID,
    *,
    no_cache: bool = False,
    lambda_function: str | None = None,
) -> AsyncIterator[AnalyzeEvent]
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"no_cache"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · bool · default: <code>{"False"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"lambda_function"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · str | None · default: <code>{"None"}</code></span></div>
</div>

**Returns**

AsyncIterator[<a href="/reference/sdk/models/runs#analyze-event"><code>AnalyzeEvent</code></a>]

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.stream_outputs` {#stream-outputs}

Yield chunks from the tar archive containing run outputs.

```python Signature
async def stream_outputs(run_id: UUID, *, task_ids: Sequence[str] | None = None) -> AsyncIterator[bytes]
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"task_ids"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Sequence[str] | None · default: <code>{"None"}</code></span></div>
</div>

**Returns**

AsyncIterator[bytes]

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.download_outputs` {#download-outputs}

Download run outputs into a new directory and unpack nested task tarballs.

Existing directories are refused. Extraction accepts only regular files and directories. Positive limits bound the downloaded archive, expanded bytes, and total member count.

```python Signature
async def download_outputs(
    run_id: UUID,
    output_dir: str | Path,
    *,
    task_ids: Sequence[str] | None = None,
    max_archive_bytes: int = 1073741824,
    max_expanded_bytes: int = 5368709120,
    max_entries: int = 100000,
) -> Path
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"output_dir"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · str | Path</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"task_ids"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Sequence[str] | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"max_archive_bytes"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int · default: <code>{"1073741824"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"max_expanded_bytes"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int · default: <code>{"5368709120"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"max_entries"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int · default: <code>{"100000"}</code></span></div>
</div>

**Returns**

Path

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.update_concurrency` {#update-concurrency}

Change an active run's concurrency limit. Existing tasks continue running.

```python Signature
async def update_concurrency(run_id: UUID, *, concurrency: int) -> UpdateRunConcurrencyResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"concurrency"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · int</span></div>
</div>

**Returns**

<a href="/reference/sdk/models/runs#update-run-concurrency-response"><code>UpdateRunConcurrencyResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.stop` {#stop}

Stop a run or selected tasks.

```python Signature
async def stop(run_id: UUID, *, force: bool = False, task_ids: Sequence[str] | None = None) -> StopRunResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"force"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · bool · default: <code>{"False"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"task_ids"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Sequence[str] | None · default: <code>{"None"}</code></span></div>
</div>

**Returns**

<a href="/reference/sdk/models/runs#stop-run-response"><code>StopRunResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.resume` {#resume}

Resume unfinished work for a run.

```python Signature
async def resume(
    run_id: UUID,
    *,
    concurrency: int | None = None,
    task_ids: Sequence[str] | None = None,
    secrets: Mapping[str, str] | None = None,
    service_headers: Mapping[str, str] | None = None,
    from_scratch: bool = False,
    benchmark_url: str | None = None,
) -> RetryOrResumeRunResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"concurrency"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"task_ids"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Sequence[str] | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"secrets"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Mapping[str, str] | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"service_headers"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Mapping[str, str] | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"from_scratch"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · bool · default: <code>{"False"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"benchmark_url"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · str | None · default: <code>{"None"}</code></span></div>
</div>

**Returns**

<a href="/reference/sdk/models/runs#retry-or-resume-run-response"><code>RetryOrResumeRunResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).

## `client.runs.retry` {#retry}

Retry failed or selected work for a run.

```python Signature
async def retry(
    run_id: UUID,
    *,
    concurrency: int | None = None,
    task_ids: Sequence[str] | None = None,
    secrets: Mapping[str, str] | None = None,
    service_headers: Mapping[str, str] | None = None,
    from_scratch: bool = False,
    benchmark_url: str | None = None,
) -> RetryOrResumeRunResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"run_id"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · UUID</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"concurrency"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"task_ids"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Sequence[str] | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"secrets"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Mapping[str, str] | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"service_headers"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · Mapping[str, str] | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"from_scratch"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · bool · default: <code>{"False"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"benchmark_url"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · str | None · default: <code>{"None"}</code></span></div>
</div>

**Returns**

<a href="/reference/sdk/models/runs#retry-or-resume-run-response"><code>RetryOrResumeRunResponse</code></a>

For examples, see the [Python SDK guide](/sdk/runs).
