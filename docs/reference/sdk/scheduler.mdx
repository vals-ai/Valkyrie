---
title: "Scheduler resource"
description: "Inspect queue positions and active tasks in the authenticated organization."
---

{/* Generated by scripts/generate_reference.py; do not edit. */}

Access these methods through `client.scheduler`.

## `client.scheduler.overview` {#overview}

Fetch live totals and up to 1–200 entries per waiting or active page.

Offsets are nonnegative integers, defaulting to zero. Advance each list with its waiting_next_offset or active_next_offset until null. Capped flags indicate more entries after this page. Totals are not capped. Pages are live: queue changes between calls can repeat or skip entries.

```python Signature
async def overview(
    *,
    waiting_limit: int = 100,
    active_limit: int = 100,
    waiting_offset: int = 0,
    active_offset: int = 0,
) -> SchedulerOverviewResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"waiting_limit"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int · default: <code>{"100"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"active_limit"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int · default: <code>{"100"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"waiting_offset"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int · default: <code>{"0"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"active_offset"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int · default: <code>{"0"}</code></span></div>
</div>

**Returns**

<a href="/reference/sdk/models/scheduler#scheduler-overview-response"><code>SchedulerOverviewResponse</code></a>

For examples, see the [Python SDK guide](/sdk/resources).
