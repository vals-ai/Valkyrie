---
title: "Agents resource"
description: "Async operations for the configured deployment's shared agent library."
---

{/* Generated by scripts/generate_reference.py; do not edit. */}

Access these methods through `client.agents`.

## `client.agents.list` {#list}

List uploaded agents in the configured shared library.

```python Signature
async def list() -> AgentsResponse
```

**Returns**

<a href="/reference/sdk/models/agents#agents-response"><code>AgentsResponse</code></a>

For examples, see the [Python SDK guide](/sdk/resources).

## `client.agents.download_url` {#download-url}

Create a temporary download URL for an uploaded agent.

```python Signature
async def download_url(name: str) -> AgentDownloadURLResponse
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"name"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · str</span></div>
</div>

**Returns**

<a href="/reference/sdk/models/agents#agent-download-url-response"><code>AgentDownloadURLResponse</code></a>

For examples, see the [Python SDK guide](/sdk/resources).

## `client.agents.push` {#push}

Bundle a directory containing contract.yaml or contract.yml and upload it through Tracker.

name defaults to the contract name and replaces that library alias if it exists.

```python Signature
async def push(agent_path: str | Path, *, name: str | None = None) -> AgentEntry
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"agent_path"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · str | Path</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"name"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · str | None · default: <code>{"None"}</code></span></div>
</div>

**Returns**

<a href="/reference/sdk/models/agents#agent-entry"><code>AgentEntry</code></a>

For examples, see the [Python SDK guide](/sdk/resources).

## `client.agents.download` {#download}

Download and safely extract an agent into output_dir/name.

output_dir defaults to the current directory; overwrite replaces an existing agent directory only after archive validation succeeds.

max_archive_bytes limits ZIP data; max_expanded_bytes and max_entries bound extraction. Limits must be positive.

```python Signature
async def download(
    name: str,
    output_dir: str | Path | None = None,
    *,
    overwrite: bool = False,
    max_archive_bytes: int = 1073741824,
    max_expanded_bytes: int = 5368709120,
    max_entries: int = 100000,
) -> Path
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"name"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · str</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"output_dir"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · str | Path | None · default: <code>{"None"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"overwrite"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · bool · default: <code>{"False"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"max_archive_bytes"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int · default: <code>{"1073741824"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"max_expanded_bytes"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int · default: <code>{"5368709120"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"max_entries"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · int · default: <code>{"100000"}</code></span></div>
</div>

**Returns**

Path

For examples, see the [Python SDK guide](/sdk/resources).

## `client.agents.remove` {#remove}

Remove an uploaded alias; missing agents return a 404 API error.

```python Signature
async def remove(name: str) -> AgentEntry
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"name"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · str</span></div>
</div>

**Returns**

<a href="/reference/sdk/models/agents#agent-entry"><code>AgentEntry</code></a>

For examples, see the [Python SDK guide](/sdk/resources).

## `client.agents.install` {#install}

Use local Git to clone an HTTPS GitHub repository or /tree/branch/subfolder URL and push it.

name defaults to the contract name and replaces that library alias if it exists.

```python Signature
async def install(github_url: str, *, name: str | None = None) -> AgentEntry
```

**Parameters**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"github_url"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · str</span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"name"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · str | None · default: <code>{"None"}</code></span></div>
</div>

**Returns**

<a href="/reference/sdk/models/agents#agent-entry"><code>AgentEntry</code></a>

For examples, see the [Python SDK guide](/sdk/resources).
