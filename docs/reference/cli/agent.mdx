---
title: "valkyrie agent"
description: "Complete generated reference for every `valkyrie agent` command."
---

{/* Generated by scripts/generate_reference.py; do not edit. */}

This page documents all 5 commands in the `agent` group in registration order. Use the table of contents to jump to a command.

## `valkyrie agent install` {#install}

Install an agent from a GitHub repository into your S3 bucket.

```bash Syntax
valkyrie agent install \
  <GITHUB_URL> \
  [--name|-n <TEXT>]
```

**Arguments**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"GITHUB_URL"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

**Options**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"--name, -n"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>{"text"}</code></span></div>
  <p className="mb-0 mt-1">{"Agent name (defaults to the contract name)"}</p>
</div>

Task guidance: [Agent guides](/agents/manage-agents).

---

## `valkyrie agent push` {#push}

Upload a local agent directory into your S3 bucket.

```bash Syntax
valkyrie agent push \
  <AGENT_PATH> \
  [--name|-n <TEXT>]
```

**Arguments**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"AGENT_PATH"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"path"}</code> · constraints: {"must exist; directory; readable"}</span></div>
</div>

**Options**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"--name, -n"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>{"text"}</code></span></div>
  <p className="mb-0 mt-1">{"Agent name (defaults to the contract name)"}</p>
</div>

Task guidance: [Agent guides](/agents/manage-agents).

---

## `valkyrie agent remove` {#remove}

Remove an installed agent

```bash Syntax
valkyrie agent remove \
  <AGENT_NAME>
```

**Arguments**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"AGENT_NAME"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

Task guidance: [Agent guides](/agents/manage-agents).

---

## `valkyrie agent download` {#download}

Download an installed agent

```bash Syntax
valkyrie agent download \
  <AGENT_NAME> \
  [--output-dir|-o <PATH>]
```

**Arguments**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"AGENT_NAME"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

**Options**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"--output-dir, -o"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>{"path"}</code> · default: <code>{"None"}</code> · constraints: {"directory; readable"}</span></div>
  <p className="mb-0 mt-1">{"Output directory for downloaded agent (default: current directory)"}</p>
</div>

Task guidance: [Agent guides](/agents/manage-agents).

---

## `valkyrie agent list` {#list}

List installed agents

```bash Syntax
valkyrie agent list
```

Task guidance: [Agent guides](/agents/manage-agents).
