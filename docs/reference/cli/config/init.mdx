---
title: "valkyrie config init"
description: "Initializes a config we can trust to have references to dependencies to run Valkyrie,\nthis becomes our source of truth for secrets required to run Valkyrie"
---

{/* Generated by scripts/generate_reference.py; do not edit. */}

Initializes a config we can trust to have references to dependencies to run Valkyrie,
this becomes our source of truth for secrets required to run Valkyrie

<Panel>
<CodeGroup>

```bash Syntax
valkyrie config init
```

```bash HTTP hosted mode
# Example only — review before running
curl --request POST "$VALKYRIE_BASE_URL/init" \
  --header "X-Api-Key: $VALS_API_KEY"
```

</CodeGroup>
</Panel>

**HTTP hosted mode:** Hosted setup validates the API key with this request. Self-hosted setup does not call Tracker.

For task guidance, see [Config guides](/get-started/configuration).
