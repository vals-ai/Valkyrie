---
title: "valkyrie config service list"
description: "List hosted and custom benchmark services."
---

{/* Generated by scripts/generate_reference.py; do not edit. */}

List hosted and custom benchmark services.

<Panel>
<CodeGroup>

```bash Syntax
valkyrie config service list
```

```bash HTTP catalog
curl --request GET "$VALKYRIE_BASE_URL/benchmark-services" \
  --header "X-Api-Key: $VALS_API_KEY" \
  --header "X-Harness-Aws-Access-Key-Id: $AWS_ACCESS_KEY_ID" \
  --header "X-Harness-Aws-Secret-Access-Key: $AWS_SECRET_ACCESS_KEY" \
  --header "X-Harness-Aws-Default-Region: $AWS_DEFAULT_REGION" \
  --header "X-Harness-S3-Bucket: $S3_BUCKET" \
  --header "X-Harness-Log-Group: $LOG_GROUP" \
  --header "X-Harness-Log-Retention-Policy: $LOG_RETENTION_POLICY"
# For temporary credentials, add:
#   --header "X-Harness-Aws-Session-Token: $AWS_SESSION_TOKEN"
```

```bash HTTP health check
curl --request POST "$VALKYRIE_BASE_URL/benchmark-services" \
  --header "X-Api-Key: $VALS_API_KEY" \
  --header "X-Harness-Aws-Access-Key-Id: $AWS_ACCESS_KEY_ID" \
  --header "X-Harness-Aws-Secret-Access-Key: $AWS_SECRET_ACCESS_KEY" \
  --header "X-Harness-Aws-Default-Region: $AWS_DEFAULT_REGION" \
  --header "X-Harness-S3-Bucket: $S3_BUCKET" \
  --header "X-Harness-Log-Group: $LOG_GROUP" \
  --header "X-Harness-Log-Retention-Policy: $LOG_RETENTION_POLICY" \
  --header "Content-Type: application/json" \
  --data @- <<'JSON'
{
  "services": [
    {
      "name": "benchmark-name",
      "url": "https://benchmark.example.com",
      "auth_header_name": null,
      "auth_secret_name": null
    }
  ]
}
JSON
# For temporary credentials, add:
#   --header "X-Harness-Aws-Session-Token: $AWS_SESSION_TOKEN"
```

</CodeGroup>
</Panel>

**HTTP health check:** The CLI sends this request only when the merged hosted/custom catalog contains services.

For task guidance, see [Config guides](/get-started/configuration).
