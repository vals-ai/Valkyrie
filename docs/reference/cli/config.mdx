---
title: "valkyrie config"
description: "Complete generated reference for every `valkyrie config` command."
---

{/* Generated by scripts/generate_reference.py; do not edit. */}

This page documents all 13 commands in the `config` group in registration order. Use the table of contents to jump to a command.

## `valkyrie config auth set` {#auth-set}

Set an auth credential for a benchmark service.

```bash Syntax
valkyrie config auth set \
  <NAME> \
  <CREDENTIAL>
```

```bash Example
# Example only, review before running
valkyrie config auth set \
  benchmark-name \
  $BENCHMARK_CREDENTIAL
```

**Arguments**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"NAME"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"CREDENTIAL"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

Task guidance: [Config guides](/get-started/configuration).

---

## `valkyrie config auth remove` {#auth-remove}

Remove an auth credential for a benchmark service.

```bash Syntax
valkyrie config auth remove \
  <NAME>
```

```bash Example
# Example only, review before running
valkyrie config auth remove swebench
```

**Arguments**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"NAME"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

Task guidance: [Config guides](/get-started/configuration).

---

## `valkyrie config auth list` {#auth-list}

List all configured benchmark auth credentials.

```bash Syntax
valkyrie config auth list
```

Task guidance: [Config guides](/get-started/configuration).

---

## `valkyrie config init` {#init}

Create the Valkyrie config with the credentials and endpoints a run needs.

```bash Syntax
valkyrie config init
```

Task guidance: [Config guides](/get-started/configuration).

---

## `valkyrie config set` {#set}

Set a single key in the Valkyrie config.

```bash Syntax
valkyrie config set \
  <KEY> \
  <VALUE>
```

```bash Example
# Example only, review before running
valkyrie config set \
  AWS_DEFAULT_REGION \
  us-west-2
```

**Arguments**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"KEY"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"VALUE"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

Task guidance: [Config guides](/get-started/configuration).

---

## `valkyrie config remove` {#remove}

Remove a single key from the Valkyrie config.

```bash Syntax
valkyrie config remove \
  <KEY>
```

```bash Example
# Example only, review before running
valkyrie config remove AWS_DEFAULT_REGION
```

**Arguments**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"KEY"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

Task guidance: [Config guides](/get-started/configuration).

---

## `valkyrie config provider set` {#provider-set}

Set a named sandbox provider secret.

```bash Syntax
valkyrie config provider set \
  <NAME> \
  <SECRET_NAME>
```

```bash Example
# Example only, review before running
valkyrie config provider set \
  daytona \
  DaytonaSecrets
```

**Arguments**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"NAME"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"SECRET_NAME"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

Task guidance: [Config guides](/get-started/configuration).

---

## `valkyrie config provider default` {#provider-default}

Set the default sandbox provider.

```bash Syntax
valkyrie config provider default \
  <NAME>
```

```bash Example
# Example only, review before running
valkyrie config provider default modal
```

**Arguments**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"NAME"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

Task guidance: [Config guides](/get-started/configuration).

---

## `valkyrie config provider remove` {#provider-remove}

Remove a named sandbox provider secret.

```bash Syntax
valkyrie config provider remove \
  <NAME>
```

```bash Example
# Example only, review before running
valkyrie config provider remove modal
```

**Arguments**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"NAME"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

Task guidance: [Config guides](/get-started/configuration).

---

## `valkyrie config provider list` {#provider-list}

List all configured sandbox providers.

```bash Syntax
valkyrie config provider list
```

Task guidance: [Config guides](/get-started/configuration).

---

## `valkyrie config service set` {#service-set}

Set a custom URL for a benchmark service (creates or updates).

```bash Syntax
valkyrie config service set \
  <NAME> \
  <URL>
```

```bash Example
# Example only, review before running
valkyrie config service set \
  swebench \
  https://my-tunnel.ngrok.io
```

**Arguments**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"NAME"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"URL"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

Task guidance: [Config guides](/get-started/configuration).

---

## `valkyrie config service remove` {#service-remove}

Remove a custom URL override for a benchmark service.

```bash Syntax
valkyrie config service remove \
  <NAME>
```

```bash Example
# Example only, review before running
valkyrie config service remove swebench
```

**Arguments**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"NAME"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">required · <code>{"text"}</code></span></div>
</div>

Task guidance: [Config guides](/get-started/configuration).

---

## `valkyrie config service list` {#service-list}

List hosted and custom benchmark services.

```bash Syntax
valkyrie config service list
```

Task guidance: [Config guides](/get-started/configuration).
