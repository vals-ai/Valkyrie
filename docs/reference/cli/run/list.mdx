---
title: "valkyrie run list"
description: "List runs by providing filter values."
---

{/* Generated by scripts/generate_reference.py; do not edit. */}

List runs by providing filter values.

<Panel>
<CodeGroup>

```bash Syntax
valkyrie run list \
  [--agent-name <TEXT>] \
  [--benchmark-name <TEXT>] \
  [--model <TEXT>] \
  [--dataset <TEXT>] \
  [--label|-l <TEXT>] \
  [--status <CHOICE>] \
  [--order-by <CHOICE>] \
  [--started-by <TEXT>] \
  [--format <CHOICE>] \
  [--all]
```

```bash Example
valkyrie run list \
  --agent-name claude_code \
  --benchmark-name swebench \
  --status IN_PROGRESS \
  --order-by DESC
```

```bash HTTP primary
curl --get "$VALKYRIE_BASE_URL/fetch-benchmarks" \
  --header "X-Api-Key: $VALS_API_KEY" \
  --header "X-Harness-Aws-Access-Key-Id: $AWS_ACCESS_KEY_ID" \
  --header "X-Harness-Aws-Secret-Access-Key: $AWS_SECRET_ACCESS_KEY" \
  --header "X-Harness-Aws-Default-Region: $AWS_DEFAULT_REGION" \
  --header "X-Harness-S3-Bucket: $S3_BUCKET" \
  --header "X-Harness-Log-Group: $LOG_GROUP" \
  --header "X-Harness-Log-Retention-Policy: $LOG_RETENTION_POLICY" \
  --data-urlencode "agent_name=agent-name" \
  --data-urlencode "order_by=desc" \
  --data-urlencode "limit=5" \
  --data-urlencode "offset=0"
# For temporary credentials, add:
#   --header "X-Harness-Aws-Session-Token: $AWS_SESSION_TOKEN"
```

</CodeGroup>
</Panel>

## Options

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">--agent-name</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>text</code></span></div>
  <p className="mb-0 mt-1">Agent name (e.g., claude_code)</p>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">--benchmark-name</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>text</code></span></div>
  <p className="mb-0 mt-1">Benchmark name (e.g., swebench)</p>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">--model</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>text</code></span></div>
  <p className="mb-0 mt-1">Model name (e.g., anthropic/claude-sonnet-4-20250514)</p>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">--dataset</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>text</code></span></div>
  <p className="mb-0 mt-1">Dataset name (e.g., default, terminal-bench-2.1)</p>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">--label, -l</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>text</code></span></div>
  <p className="mb-0 mt-1">Run label</p>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">--status</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>choice</code> · default: <code>None</code> · constraints: one of `IN_PROGRESS`, `STOPPING`, `STOPPED`, `FINISHED`, `ERROR`; case-insensitive</span></div>
  <p className="mb-0 mt-1">Status of the benchmarks to fetch (e.g., in_progress, finished, error)</p>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">--order-by</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>choice</code> · default: <code>'desc'</code> · constraints: one of `asc`, `desc`; case-insensitive</span></div>
  <p className="mb-0 mt-1">Order by the benchmarks to fetch (e.g., desc, asc)</p>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">--started-by</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>text</code> · default: <code>None</code></span></div>
  <p className="mb-0 mt-1">Comma-separated list of starter emails (e.g., alice@example.com,bob@example.com). Case-insensitive.</p>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">--format</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>choice</code> · default: <code>'text'</code> · constraints: one of `text`, `json`; case-insensitive</span></div>
  <p className="mb-0 mt-1">Output format. Machine-readable JSON requires --all.</p>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">--all</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>boolean</code> · default: <code>False</code></span></div>
  <p className="mb-0 mt-1">Fetch every matching run without interactive paging. Requires --format json.</p>
</div>

For task guidance, see [Run guides](/runs/start).
