---
title: "valkyrie queue"
description: "Complete generated reference for every `valkyrie queue` command."
---

{/* Generated by scripts/generate_reference.py; do not edit. */}

This page documents all 1 commands in the `queue` group in registration order. Use the table of contents to jump to a command.

## `valkyrie queue status` {#status}

Show queue priorities, positions, and active tasks for your organization.

```bash Syntax
valkyrie queue status \
  [--waiting-limit <INTEGER>] \
  [--active-limit <INTEGER>] \
  [--format <CHOICE>]
```

**Options**

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"--waiting-limit"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>{"integer"}</code> · default: <code>{"100"}</code> · constraints: {">= 1; <= 200"}</span></div>
  <p className="mb-0 mt-1">{"Maximum waiting task entries."}</p>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"--active-limit"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>{"integer"}</code> · default: <code>{"100"}</code> · constraints: {">= 1; <= 200"}</span></div>
  <p className="mb-0 mt-1">{"Maximum active task entries."}</p>
</div>

<div className="border-b border-gray-200 py-3 dark:border-gray-800">
  <div className="flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1"><span className="break-all font-mono text-sm">{"--format"}</span><span className="min-w-0 break-words text-sm text-gray-500 dark:text-gray-400">optional · <code>{"choice"}</code> · default: <code>{"'text'"}</code> · constraints: {"one of `text`, `json`; case-insensitive"}</span></div>
  <p className="mb-0 mt-1">{"Output format. JSON includes bounded task entries and truncation flags."}</p>
</div>

Task guidance: [Queue guides](/runs/monitor#inspect-the-priority-queue).
