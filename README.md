# Agentic Harness

Define your agent in `agents/`, add benchmarks as git submodules in `benchmarks/`, and run evaluations.

```bash
python src/runner.py --agent-dir agents/sample_agent/ --benchmark my_benchmark
```

**Setup:** `make install`
**Style:** `make style`
